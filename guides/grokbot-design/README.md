# Grok Bot for Design

A standalone guide for product teams using Grok Bot to explore and implement design changes. The examples use a dashboard feedback card; the prompts can be adapted to any screen or component.

## Use the guide

- [Read the guide on GitHub](grokbot-design-guide.md).
- [Open the interactive website guide](https://designproject.io/grokbot-design-guide/) after the companion website PR deploys.
- Download [the standalone HTML](grokbot-design-guide.html) using GitHub’s **Download raw file** button, then open it in a browser. Fonts, styles, and scripts are embedded; no installation or server is needed.
- [Get all nine prompts](grokbot-design-prompts.txt).

The interactive version includes copy buttons, a downloadable prompt file, seven progress checkpoints saved in the browser, and a print layout. **Save PDF** opens the browser’s print dialog.

This is a human-readable guide. It does not install an agent skill through `npx skills add`.

## Maintaining this resource

Keep the guide, prompt file, and Markdown version in sync. The website serves the same HTML at `public/grokbot-design-guide/index.html` in `thedesignproject/designproject.io`; its curated Markdown lives at `content/markdown/pages/grokbot-design-guide.md`. Update both repositories together when changing the content.

## Font licenses

The HTML embeds Press Start 2P and VT323, matching the TDP resource styling. Both use the SIL Open Font License; the notices are included in [fonts/](fonts/).
